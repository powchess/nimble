import base58Chars from './base58-chars';
import OP_CODES from './opcodes';
import SIGHASH_FLAGS from './sighash-flags';

const constants = {
	base58Chars,
	opcodes: OP_CODES,
	sighashFlags: SIGHASH_FLAGS,
};

export default constants;
